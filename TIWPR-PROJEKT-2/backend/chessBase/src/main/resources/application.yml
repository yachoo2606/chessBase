server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:32768/chessbase
    username: postgres
    password: postgrespw
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: 48404D635166546A576D5A7134743777217A25432A462D4A614E645267556B58
      expiration: 60000 # 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days